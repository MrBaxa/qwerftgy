let name = prompt("Введите ваше имя:");
let spaceId = prompt("Введите Space ID:");
let spacePassword = prompt("Введите Space Password:");

let userInfo = {
    name: name,
    ID: spaceId,
    password: spacePassword
};
console.log(userInfo);
